build:
  docker:
    web: Dockerfile
release:
  command:
    - docker build -t bbp-jiriki-frontend .
    - heroku container:push web -a bbp-jiriki-frontend
    - heroku container:release web -a bbp-jiriki-frontend
